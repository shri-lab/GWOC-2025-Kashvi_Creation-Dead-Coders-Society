<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600&display=swap" rel="stylesheet">
    <title>Owner Dashboard</title>
    <link rel="stylesheet" href="/style.css">
    <style>
        * {
            font-family: "Inter", sans-serif;
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            background: #f0f0f0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            padding: 20px;
        }
        .container {
            text-align: center;
            background: white;
            padding: 30px;
            border-radius: 12px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }
        .header {
            font-size: 2rem;
            font-weight: 600;
            color: #333;
            margin-bottom: 20px;
        }
        .btn {
            display: block;
            width: 200px;
            padding: 12px;
            margin: 10px auto;
            font-size: 16px;
            text-decoration: none;
            color: white;
            background: #333;
            border-radius: 6px;
            transition: 0.3s;
        }
        .btn:hover {
            background: #555;
        }
        #home-btn{
            position: absolute;
            right: 10px;
            top:10px;
            

        }
        button{
            height:5vh;
            background:rgba(0,0,0,0);
            border:2px solid #000;
            color:#000;
            border-radius: 20px;
            width:10vh;
        }
        button:hover{
            cursor: pointer;
            background: #000;
            color:#fff;
        }
    </style>
</head>
<body>
    <a href="/" id = "home-btn"><button>Home</button></a>
    <div class="container">
        <div class="header">Dashboard</div>
        <a href="/writeBlog" class="btn">Write a Blog</a>
        <a href="/owner" class="btn">Manage Reviews</a>
        <a href="/upload" class="btn">Upload a new product</a>
    </div>
</body>
</html>
